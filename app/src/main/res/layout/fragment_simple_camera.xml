<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- Fullscreen Camera View Container -->
    <FrameLayout
        android:id="@+id/cameraViewContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black" />

    <!-- UVC Logo (centered, can be hidden if needed) -->
    <ImageView
        android:id="@+id/uvcLogoIv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:src="@mipmap/ic_uvc"
        android:visibility="gone" />

    <!-- Camera Status Display (top-center) -->
    <TextView
        android:id="@+id/cameraStatusTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="16dp"
        android:text="Status: Camera Offline"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:background="#80000000"
        android:padding="12dp"
        android:fontFamily="sans-serif-medium" />

    <!-- Frame Rate Display (optional, top-left corner) -->
    <TextView
        android:id="@+id/frameRateTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:background="#80000000"
        android:padding="8dp"
        android:visibility="gone" />

    <!-- Error Message Container (centered) -->
    <LinearLayout
        android:id="@+id/errorMessageContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone">

        <!-- Error Title -->
        <TextView
            android:id="@+id/errorTitleTv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Connect your USB camera"
            android:textColor="@color/white"
            android:textSize="24sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="16dp"
            android:gravity="center" />

        <!-- Error Message -->
        <TextView
            android:id="@+id/errorMessageTv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Seems like there is problem with USB camera feed, please check the USB connection or power supply for the camera"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:maxWidth="400dp" />

    </LinearLayout>

</FrameLayout>
